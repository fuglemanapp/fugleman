"use client";
import { Download, X } from "lucide-react";
import { useEffect, useState } from "react";
type InstallPrompt = Event & { prompt: () => Promise<void>; userChoice: Promise<{ outcome: "accepted" | "dismissed" }> };
export function InstallAppPrompt() { const [prompt, setPrompt] = useState<InstallPrompt | null>(null); const [hidden, setHidden] = useState(true); useEffect(() => { const show = (event: Event) => { event.preventDefault(); setPrompt(event as InstallPrompt); setHidden(localStorage.getItem("whatspent-install-dismissed") === "1"); }; window.addEventListener("beforeinstallprompt", show); return () => window.removeEventListener("beforeinstallprompt", show); }, []); if (!prompt || hidden) return null; return <div className="fixed bottom-4 left-4 right-4 z-50 mx-auto flex max-w-md items-center gap-3 rounded-2xl border border-[#cde7d6] bg-white p-3 shadow-xl"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#e8f8ee] text-[#087d3c]"><Download className="h-4 w-4" /></span><p className="flex-1 text-sm font-semibold text-[#214235]">Instale o WhatSpent no seu dispositivo.</p><button onClick={() => void prompt.prompt()} className="rounded-xl bg-[#087d3c] px-3 py-2 text-xs font-bold text-white">Instalar</button><button aria-label="Fechar" onClick={() => { localStorage.setItem("whatspent-install-dismissed", "1"); setHidden(true); }} className="text-[#658075]"><X className="h-4 w-4" /></button></div>; }
